import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  root: {
    width: 264,
  },
  media: {
    height: 160,
  },
  subtitle: {
    minHeight: 48,
    display: "flex",
    alignItems: "center",
    fontSize: "1.09rem",
    lineHeight: 1.43,
  },
});

export { useStyles };
